<UserControl x:Class="ControlSystem.Components.DeviceTile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ControlSystem.Components"
             mc:Ignorable="d" 
             x:Name="deviceTile"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Style="{StaticResource DeviceTileBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" x:Name="DeviceName" Text="TITLE" Style="{StaticResource DeviceTileTitle}"/>
                <TextBlock Grid.Column="1" x:Name="DeviceType" Text="TYPE" Style="{StaticResource DeviceTileTitle}"/>
            </Grid>

            <TextBlock Grid.Row="1" x:Name="DeviceIcon" Text="ICON" Style="{StaticResource DeviceTileIcon}"></TextBlock>
    
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="On/Off" Style="{StaticResource DeviceTileOnOffText}"/>
                
                <ToggleButton Grid.Column="1" Style="{StaticResource DeviceTileOnOffSwitch}"/>
            </Grid>

        </Grid>
    </Border>
</UserControl>
